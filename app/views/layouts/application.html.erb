<!DOCTYPE html>
<html>
<head>
  <title>Academy Air</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	
	<div id="container">
		<header>
			<h1><%= link_to "Academy Air", root_url %></h1>
			<nav>
				<ul>
					
					<% if logged_in? %>
						<li><%= link_to "Admin Panel", reservations_url if current_user.admin? %></li>
						<li><%= link_to "View My Profile", user_url(current_user) %></li>
						<li><%= link_to "Help", help_url %></li>
						<li><%= link_to "Sign Out", signout_url, :method => :delete %></li>
					<% else %>
						<li><%= link_to "Sign In", new_session_url %></li>
						<li><%= link_to "Help", help_url %></li>
						<li><%= link_to "Sign Up", new_user_url  %>
						<% end %>
				</ul>
			</nav>
			<% if logged_in? %>
				<p>Howdy <%= current_user.name %></p>
			<% end %>
		</header>

		<div id="content">
			<%= notice %>

			<%= yield %>
		</div>
		
		<footer>
			&copy;2012 Academy Air, Inc.  Got comments or suggestions? Keep 'em to yourself!
		</footer>
	</div>
</body>
</html>
